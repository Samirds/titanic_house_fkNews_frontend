import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class PredictionPageFN extends StatelessWidget {
  const PredictionPageFN({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text("Work is Under Progress"),),
      ),
    );
  }
}
